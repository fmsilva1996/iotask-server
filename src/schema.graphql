const { buildSchema } = require('graphql')

module.exports = buildSchema(`
  type Query {
    getTasks: [Task!]!
    getTask(taskId: String!): Task!
  }

  enum TaskStatus {
    TODO
    IN_PROGRESS
    DONE
  }

  type Task {
    id: ID!
    title: String!
    description: String!
    status: TaskStatus
  }

`)

